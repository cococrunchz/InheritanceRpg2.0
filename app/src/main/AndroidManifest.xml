<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".view.fighterScreen"></activity>
        <activity android:name=".view.wandererScreen" />
        <activity android:name=".view.swordsmanScreen" />
        <activity android:name=".view.mageScreen" />
        <activity android:name=".view.rangerScreen" />
        <activity android:name=".view.monsterMenu" />
        <activity android:name=".view.heroMenu" />
        <activity android:name=".view.homeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>