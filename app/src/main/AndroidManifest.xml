<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".demonScreen"></activity>
        <activity android:name=".golemScreen" />
        <activity android:name=".trollScreen" />
        <activity android:name=".slimeScreen" />
        <activity android:name=".fighterScreen" />
        <activity android:name=".wandererScreen" />
        <activity android:name=".swordsmanScreen" />
        <activity android:name=".mageScreen" />
        <activity android:name=".rangerScreen" />
        <activity android:name=".monsterMenu" />
        <activity android:name=".heroMenu" />
        <activity android:name=".homeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>